Solving issues with fonts on breaktimer:

Remove & recreate cache on the system
rm -rf ~/.cache/fontconfig 
sudo fc-cache -r -v

Remove the cached config from breaktimer
find ~/snap/breaktimer/ -name 'fontconfig'
rm -rf ~/snap/breaktimer/common/.cache/fontconfig/

Restart breaktimer. If doesn't work, reinstall breaktimer

Source:
https://askubuntu.com/questions/1224125/font-characters-displayed-as-squares-in-ubuntu-18-04

Insert the intelephense license key at $HOME/intelephense/licence.txt
